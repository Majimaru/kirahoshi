<% if user_signed_in? %>
  <nav class="col-md-8 mx-auto mb-md-5 navbar navbar-expand navbar-dark bg-secondary fixed-bottom nav2 rounded-pill">
    <ul class="w-100 navbar-nav nav-justified text-light">
      <li class="nav-item">
        <%= link_to "マイページ", user_path(current_user), class: "w-100 btn rounded-pill" %>
      </li>
      <li class="nav-item">
        <%= link_to "投稿", new_post_path, class: "w-100 btn rounded-pill" %>
      </li>
      <li class="nav-item">
        <%= link_to user_path(current_user), class: "w-100 btn rounded-pill" do %>
          レビュー
        <% end %>
      </li>
    </ul>
  </nav>
<% end %>