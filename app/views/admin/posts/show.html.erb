<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-10 col-lg-8 mx-auto mt-2 pt-4 text-light">
      <h3 class="mb-4 pb-1 px-3 border-start border-5 border-warning">投稿詳細</h3>
      
      <table class="table table-borderless admin-post-table">
        <tr class="mb-4">
          <td class="col-4 report-td">投稿ID</td>
          <td class="col-8 report-td"><%= @post.id %></td>
        </tr>
        <tr class="mb-4">
          <td class="report-td">ユーザー名</td>
          <td class="report-td"><%= @post.user.user_name %></td>
        </tr>
        <tr class="mb-4">
          <td class="report-td">メッセージ</td>
          <td class="report-td"><%= @post.message %></td>
        </tr>
        
        <!--投稿画像が存在する場合は表示-->
        <% if @post.post_image.attached? %>
          <tr>
            <td class="report-td">投稿画像</td>
            <td class="report-td">
              <%= image_tag @post.post_image, class: "mt-3 rounded post-image" %>
            </td>
          </tr>
        <% end %> 
        
        <tr>
          <td class="report-td">レビュー項目</td>
          <td class="report-td"><%= @post.review_item %></td>
        </tr>
      </table>
      
      <div class="row mt-5">
        <!--通報ステータスが未対応の場合のみ「問題なし」ボタンを表示する-->
        <% if @report.status == "wait_response" %>
          <%= link_to "問題なし", admin_post_report_path(@report), method: :patch, 
            class: "col-3 me-4 btn btn-success rounded-pill", :data => {:confirm => '通報ステータスを「対応済み」にしてもよろしいですか？'} %>
        <% end %>
        
        <%= link_to "削除する", admin_post_path(@post), method: :delete, 
          class: "col-3 btn btn-outline-danger rounded-pill border-3", :data => {:confirm => '削除してもよろしいですか？'} %>
      </div>
    </div>
  </div>
</div>

