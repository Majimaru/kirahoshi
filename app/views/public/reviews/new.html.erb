<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-9 col-lg-6 mb-5 mx-auto text-light">
      <%= render "public/posts/top_navbar", view: "review" %>
      
      <!--投稿データが0件の時-->
      <% if @posts.count == 0 %>
        <!--検索を行う前の遷移の時-->
        <% if @search.nil? %>
          <h5 class="mt-5 text-center">投稿データがありません</h5>
          
        <% else %>
          <h5 class="mt-5 text-center">検索結果がありません</h5>
        <% end %>
        
      <!--投稿データが1件以上の時-->
      <% else %>
        <!--検索を行う前の遷移の時、上に余白を開ける-->
        <% if @search.nil? %>
          <div class="mt-4"></div>
        <% end %>
        
        <!--投稿一覧表示-->
        <%= render "public/posts/index", posts: @posts, view: "review" %>
      <% end %>
    </div>
  </div>
</div>